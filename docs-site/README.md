# Industrial Data Platform Docs Site

Internal Starlight site over curated repository documentation.

```bash
npm install
npm run dev
npm run build
```

The site is intentionally a navigation layer. Canonical source files remain in
`docs/`, `apps/*/docs/`, and `arch/likec4/`.
