# Claude Instructions

This repository uses `AGENTS.md` as the canonical agent instruction file.

Before making changes:

1. Read `AGENTS.md`.
2. Read the closest scoped `AGENTS.md` for the paths you will edit.
3. Check `docs/agents/module-map.yaml` for ownership and validation commands.

Do not duplicate or override repository policy here. If instructions need to
change, update `AGENTS.md` and the relevant scoped guide.
