apiVersion: 1

providers:
  - name: idp-service-operations-dashboards
    orgId: 1
    folder: Service Operations
    folderUid: idp-service-operations
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
