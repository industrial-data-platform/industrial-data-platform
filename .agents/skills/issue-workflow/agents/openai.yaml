interface:
  display_name: "Issue Workflow"
  short_description: "Implement existing repository issues"
  default_prompt: "Use $issue-workflow to implement an existing issue in this repository."

policy:
  allow_implicit_invocation: true
